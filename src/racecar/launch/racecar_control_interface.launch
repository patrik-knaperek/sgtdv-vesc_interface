<!-- -*- mode: XML -*- -->
<launch>

    <!--<rosparam file="$(find racecar)/config/vesc.yaml" command="load"/>
    <node pkg="vesc_driver" type="vesc_driver_node" name="vesc_driver"/>-->

    <!-- vehicle geometry parameters, footprint, and static transforms -->
    <include file="$(find racecar)/launch/includes/vehicle_geometry.launch.xml"/>

    <!-- start electronic speed controller driver -->
    <include file="$(find racecar)/launch/includes/vesc.launch.xml" />

    <node pkg="tf" type="static_transform_publisher" name="odom_to_map" 
    args="0 0.0 0 0 0 0 map odom 10" />

    <group ns="sgt">
        <rosparam file="$(find racecar)/config/sgt.yaml" command="load" />
    </group>

    <node output="screen" pkg="racecar" type="racecar" name="racecar_control_interface"/>

</launch>
